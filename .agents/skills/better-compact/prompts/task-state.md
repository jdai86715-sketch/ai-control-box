仅当 Better Compact 已为当前任务选定唯一项目时，创建或静默维护该项目根目录的 `TASK_STATE.md`。不要仅因本提示被注入而创建状态文件；项目不唯一时先询问用户选择一级项目目录。

`TASK_STATE.md` 记录目标、已确认边界、已排除项、当前状态和下一步。它是状态快照，不是聊天日志；关键状态变化时合并、替换或删除过期内容。

后续继续任务前先读取该文件。若与代码、测试或其他可验证事实冲突，以事实为准并及时修正。不要记录密钥、令牌、密码或隐私信息。
